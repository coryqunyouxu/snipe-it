pipeline {
    agent any
    stages {
        stage('Deploy to Kubernetes') {
            steps {
                script {
                    dir('metalib') {
                        sh 'microk8s kubectl apply -f metallb-addressconfig.yaml'
                        
                        
                    }
                }
            }
        }
    }
}
