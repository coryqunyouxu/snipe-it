pipeline {
    agent any
    stages {
        stage('Deploy to Kubernetes') {
            steps {
                script {
                    
                        sh 'microk8s kubectl get all -n ingress-nginx'
                        
                        
                    
                }
            }
        }
    }
}
