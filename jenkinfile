pipeline {
    agent any
    stages {
        stage('apply') {
            steps {
                script {
                    sh 'microk8s kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.5.1/deploy/static/provider/cloud/deploy.yaml'
                }
            }
        }
    }
}
