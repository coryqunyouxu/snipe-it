apiVersion: v1
kind: PersistentVolume   
metadata:
  name: db-pv    
spec:
  capacity:
    storage: 200Mi          
  accessModes:
  - ReadWriteOnce         
  storageClassName: db-snipeit-storage
  
